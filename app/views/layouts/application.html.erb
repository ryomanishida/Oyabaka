<!DOCTYPE html>
<html>
  <head>
    <title>Oyabaka</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/454b157adc.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Klee+One:wght@600&family=Yomogi&display=swap" rel="stylesheet">
  </head>

  <body>
    <%= yield %>
    <footer>
      <div class="col">
        <%=link_to "About", home_about_path, class: "link" %>
        <div>
          <% if session[:locale] == "en" || session[:locale].blank? %>
            <%= link_to "/home/change_language/ja", class: "link" do %>
              Japanese/日本語
            <% end %>
          <% elsif session[:locale] == "ja" %>
            <%= link_to "/home/change_language/en", class: "link" do %>
              English/英語
            <% end %>
          <% end %>
        </div>
        <p>© 2021 Oyabaka</p>
      </div>
    </footer>
  </body>
</html>
